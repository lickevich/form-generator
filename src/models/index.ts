export const inputTypes: string[] = ["text", "number", "date"];
export const buttonTypes: string[] = ["submit", "reset", "button"];

export const isCheckboxType = (type: string): boolean => type === "checkbox";
export const isRadioType = (type: string): boolean => type === "radio";
export const isTextareaType = (type: string): boolean => type === "textarea";
